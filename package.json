{
  "scripts": {
    "start": "parcel src/index.html --open --log-level 3 --global app",
    "build": "parcel build src/index.html --public-url . --detailed-report --no-cache --out-dir ./build"
  },
  "devDependencies": {
    "babel-core": "^6.26.3",
    "babel-plugin-transform-runtime": "^6.23.0",
    "babel-preset-env": "^1.7.0",
    "babel-runtime": "^6.26.0",
    "eslint": "^5.3.0",
    "parcel-bundler": "^1.9.7"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "marked": "^0.4.0"
  }
}
